# Weather_API — Surya K

Beginner-friendly Python weather app demonstrating how to call OpenWeatherMap API.

## Quick run
1. Install dependencies: `pip install -r requirements.txt`
2. Run: `python Project2.py`
3. Enter a city name and your OpenWeatherMap API key (or set OPENWEATHER_API_KEY env var).
